//! Placeholder — Task 14 builds out the tray icon and window lifecycle.
//! Task 13 only needs this module to exist so `main.rs`/`lib.rs` compiles.

pub fn setup(_app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
